clone the repo  
edit WinSand.wsb ```<HostFolder>G:\from_git\WinSand</HostFolder>``` so the HostFolder path is the repo path  
add additional shared folders here or add files to that location to sahre to sandbox  
double click WinSand.wsb  
in the sandbox open the folder C:\from_host  
double click config_me.bat  
